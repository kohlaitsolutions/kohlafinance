# Disable frozen lockfile to allow fresh dependency resolution
frozen-lockfile=false
prefer-frozen-lockfile=false
# Use npm instead of pnpm for more reliable deployments
package-manager=npm
# Optimize installation for CI/CD environments
registry=https://registry.npmjs.org/
save-exact=false
# Disable package-lock generation conflicts
package-lock=false
